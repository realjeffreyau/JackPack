node_modules/
.expo/
dist/
dist-check/
.validate-out/
web-build/
*.log
.DS_Store
*.orig.*

# Local secrets (Supabase keys) — never commit
.env
.env.local

# codex-orchestrate scratch state
.codex-orchestrate/

# graphify generated reports — regenerate on demand, not source
graphify-out/
